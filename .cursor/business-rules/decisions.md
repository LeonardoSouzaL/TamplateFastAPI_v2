# Decisões de negócio e arquitetura

Registro de decisões confirmadas pelo código, documentação ou pelo time.

| Data | Domínio | Decisão | Impacto | Deve atualizar rule? |
| ---- | ------- | ------- | ------- | -------------------- |
| 2026-09-11 | Chamados Indoor / Transportes | **Alinhar só a viagem** no `READY_FOR_PICKUP`: `POST /api/v2/integration_system/status` com `status_travel` (`TRANSPORT_INDOOR_READY_TRAVEL_STATUS`, default `Pronto para coleta`), sem `status_os`. Tracking v3 permanece (não muda status). Collect/cancel **não** mandam `FINISHED`/`UNDUE`. GET prévio pula POST se type/rótulo já bater. Falha não bloqueia. | Painel Transportes sai de “aguardando estoque” para “pronto para coleta” | `215`, `270` |
| 2026-09-03 | Chamados Indoor / Transportes | **Observação no tracking READY:** `data.description` usa o `description` (senão `resume`) do `POST /transition` para `READY_FOR_PICKUP`, formato `Chamado SR… pronto para coleta - {obs}` (sem aspas). Fallback: `description` da abertura. | Tela Transportes mostra o texto que o operador digitou no pronto para coleta | `215`, `270` |
| 2026-09-02 | Chamados Indoor | **Volume cancelado permanece visível:** após `POST .../packages/{id}/cancel`, ativos recompactam 1..N; o cancelado **não some** — fica na lista com `status=CANCELLED` e o `package_number`/`label` originais (auditoria). Unique parcial só nos ativos. Ordem: ativos primeiro, cancelados no fim. | Front mostra badge “Pack cancelado”; não usa cancelado na etiqueta | `270` |
| 2026-09-01 | Chamados Indoor | **Cancel de volume recompacta numeração:** após `POST .../packages/{id}/cancel`, volumes ativos (`status ≠ CANCELLED`) passam a 1..N; `label` sequencial (`VOL-n` / `Volume n`) acompanha. `next_package_number` ignora cancelados. *(Supersedido em 2026-09-02: cancelados deixam de sair da sequência com `1_000_000 + id`; permanecem visíveis com o número original e unique parcial só nos ativos.)* | Etiqueta/tela sem buraco após excluir volume; front recarrega `GET /{id}` | `270` |
| 2026-08-25 | Chamados Indoor | **Distribuir kits já bipados (supply):** `POST /packages/distribute` cria volumes e auto-prepara as linhas supply na proporção de `planned_kits`. `kits` = soma do restante dos insumos. Serializado e `POST /packages` avulso continuam manuais. | Front não exige bipagem de supply nos volumes distribuídos | `270` |
| 2026-08-21 | Chamados Indoor / Transportes | **Create sem aprovação abre viagem + e-mails iguais ao gate:** `POST /` com `approval_required=false` chama Abertura v2 e dispara `APPROVED` (mesmo HTML/grupo do `approve`). `READY_FOR_PICKUP` continua com o e-mail de coleta. Falha da v2 → 424 e o chamado não persiste. | Front trata 424 no create; Django não reenvia | `215`, `270` |
| 2026-08-21 | Chamados Indoor | **Kanban gerencial:** `GET /stock-requests?from_location_id=0` (e/ou `to_location_id=0`) lista qualquer chamado Indoor, sem filtro de localidade — mesmo contrato dos romaneios (`location_id=0`). PA/CD continuam mandando o GAI real. Mutações inalteradas. | Front gerencial manda `0`, não o id do GAI | `270` |
| 2026-08-21 | Chamados Indoor / Supply | **Gap de reserva hard = hold aberto, não `items.reserved_quantity`.** Create/PATCH gravam `reserved_quantity=0`; cobertura incompleta só em `extra.availability_soft`. Snapshot soft na linha impedia criar hold (prepare 409 mesmo com saldo). Fulfill/list_gaps usam SUM hold. Chamados já abertos destravam no próximo prepare/ENTRY. | Prepare deixa de 409 quando a linha nasceu “reservada” sem hold | `270` |
| 2026-08-21 | Recebimento de carga | **Conferência obrigatória + comprovante:** `POST /{id}/receive` exige `items` (SKU+qty) e `receipt_url`/`signature_url` (igual coleta Indoor). Divergência continua sem bloqueio + e-mail CRÍTICO. Evidências em `extra.receipt`. Sem atalho omitir `items`. | Front faz upload Django → URLs no POST; GET expõe `receipt` | `271` |
| 2026-08-21 | Chamados Indoor | **Tipos por GAI da allowlist:** `Indoor Atendimento.client_id` = GAI CIELO de `logistic_stock_client_catalog` (mesmo `id` de `GET /clients/`). `GET /catalogs/types?client_id=` obrigatório; igualdade estrita; exclui inbound. NULL não entra. | Front manda o GAI do combo; CIELO vê Indoor; outro GAI → `[]` | `270` |
| 2026-08-20 | Chamados Indoor | **Tipos por GAI:** `Indoor Atendimento.client_id` = GAI CIELO (id real). `GET /catalogs/types?client_id=` obrigatório; filtra `logistic_stock_request_types.client_id`. Tipo exclusivo daquele cliente. | Front manda o GAI escolhido; outro cliente não vê o tipo CIELO | `270` |
| 2026-08-20 | Chamados Indoor / Supply | **Top-up de reserva hard:** ENTRY e destino de TRANSFER (e o `prepare`) completam `reserved_quantity` + hold das linhas OPEN/IN_SEPARATION com gap. Ordem: prioridade (`sort_order` DESC) → SLA (`sla_due_at` ASC) → `request.id`. Inbound e READY_FOR_PICKUP não entram. Sem EXIT. | Prepare deixa de 409 quando o estoque entra depois do OPEN; `holds[]` sobe sozinho | `260`, `270` |
| 2026-08-19 | Recebimento de carga | **Conferência com divergência:** `POST /{id}/receive` com `items` = qty real. A mais, a menos ou SKU extra não bloqueia. History `INBOUND_QTY_VARIANCE`. E-mail CRÍTICO via api-crm (mesmo grupo Indoor). Omitir `items` = conforme o aviso, sem e-mail. Swagger: tag Recebimento de carga V1 separado do Indoor. | Front mostra `variances`; Django não reenvia | `271` |
| 2026-08-19 | Chamados Indoor / Transportes | **Abertura Indoor:** `external_order_number` = `external_protocol` se preenchido, senão `SR########`. Supply `product_model={sku} - {description}` (ex. `000001 - BOBINA`; sem sku/descrição → `BOBINA`). Extra `protocol_number` continua `SR`. | Tela Transportes: OS externa do CRM + Modelo com SKU e descrição; só novos `approve` | `215`, `270` |
| 2026-08-19 | Chamados Indoor / e-mail | **api-crm `broadcast-by-group`:** stock-api envia HTML no `approve` (chamado+viagem) e no `READY_FOR_PICKUP`. `should_notify=false` para o Django não duplicar. Complemento/rejeição continuam no front. Linha: tipo (`BOBINAS` no supply) · SKU · qty. Falha do CRM não bloqueia. Key só em `.env`. | Settings `CRM_EMAIL_*`; `core/crm_email.py`; `services/stock_request_email.py` | `270` |
| 2026-08-18 | Chamados Indoor / Transportes | **Tela Transportes Indoor:** `serial_number` = qty **total** da linha; supply `product_model=BOBINA` (coluna Tipo continua `UNIQUE` — o Transportes força sem sub_itens). Paletes `ceil(qty/1600)` só em `item_control`/`extra.volumes`. | Grade/modal Editar Item: Serial=3500, Modelo=BOBINA, Controle=3 paletes | `215`, `270` |
| 2026-08-18 | Chamados Indoor / Transportes | **Paletes na abertura Indoor:** 1 palete = 1600 unidades (`ceil(qty/1600)`). **1 item por linha** do chamado; `item_control`/`extra.volumes` = nº de paletes. Tracking READY manda packages só para controle e **não** altera OS/viagem. | Cotação antecipada no `approve` sem multiplicar itens; romaneio PA→CD inalterado | `215`, `270` |
| 2026-08-18 | Chamados Indoor / Transportes | **Tracking v3 no READY_FOR_PICKUP:** envia rastro `READY_FOR_PICKUP` (evento **geral**, sem cadastro no tipo 63). `order_type=REDESPACHO_BOBINAS`, volumes no extra. Sem `EXTERNO` e sem `01-PRONTO PARA DESPACHO`. Falha não bloqueia. Diagnóstico em `GET /{id}/transport`. | Front inalterado no POST; suporte usa o GET; contrato `docs/tracking-v3-redespacho-bobinas-stock.md` | `215`, `270` |
| 2026-08-17 | Chamados Indoor | **Visões de leitura separadas:** resumo administrativo em `GET /{id}/summary` (sem itens/packages/histórico); detalhe operacional em `GET /{id}` (sem timeline); eventos só em `GET /{id}/history`. | Front: modal admin → `/summary`; operação → `GET /{id}`; timeline → `/history` | `270` |
| 2026-08-18 | Chamados Indoor / Transportes | **Pré-alerta + receive:** `POST /stock-requests/inbound-alerts` abre aviso `AWAITING_RECEIPT` (sem estoque). `POST /{id}/receive` dá ENTRY só de supply no destino. Serial aceito e ignorado. Tipo seed Recebimento de carga. | Transportes usa o MD publicado sem mudança de contrato | `271` |
| 2026-08-17 | Chamados Indoor / Transportes | **Approve abre viagem** na Abertura v2 (`created_by=ESTOQUE`, `order_type=REDESPACHO`, supply+serializado). Falha 424 mantém `AWAITING_APPROVAL`. Homolog `hg-api-transportes`. | `POST /transition` action=approve chama Transportes antes de OPEN | `215`, `270` |
| 2026-08-13 | Chamados Indoor | **Cliente do chamado:** `client_id` obrigatório no create (GAI `arancia_client`; alias `1`→CIELO). Só produtos daquele cliente nas linhas (POST/PATCH) e na bipagem. Response aninha `client`. Migration `b2c3d4e5f6a7`. | Front envia `client_id` no POST e lê `client` no GET; 409 se SKU/serial de outro cliente | `270` |
| 2026-08-12 | Chamados Indoor | **Cancel pack / unprepare:** liberar prepared (volume inteiro ou um item) ao disponível sem cancelar o chamado; pack `CANCELLED` ou prepared removido; supply baixa hold+`reserved_quantity`; serial `RESERVED`→`IN_DEPOT`. Sem realocação entre chamados. | Endpoints `/packages/{id}/cancel` e `/prepared/{id}/unprepare` | `270` |
| 2026-08-12 | Chamados Indoor | **Contrato coleta canônico B** (spec Arancia collect): manter só `POST /{id}/collect`; sem proxy `transition`→`COLLECTED`. BFF troca 1 chamada. Homolog sem rota = redeploy `release5-FluxoChamadosIndoor`, não gap de código. | Docs front (`chamados-indoor-spec` §2.4.4); rule `270` inalterada | não |
| 2026-08-10 | Chamados Indoor | **Hints de notificação por e-mail:** API não envia SMTP. Em transition (`approve` / `request_complement` / `reject` / `READY_FOR_PICKUP`) devolve `notification` + snapshot em `history.extra`. Solicitante sem e-mail (`user_ref=created_by`); responsável retirada teste = `STOCK_REQUEST_PICKUP_NOTIFY_EMAIL` (`ti@c-trends.com.br`). Sem Qualitor; não bloqueia go-live. | Campo `notification` na response; config pickup e-mail | `270` |
| 2026-08-07 | Chamados Indoor | **Efeito estoque na coleta** (backlog MVP fechado): handoff = único momento de saída física. Item → `IN_TRANSIT` + `STOCK_REQUEST_COLLECT`; supply EXIT + release holds; indicadores = reflexo de saldo/status/histórico (sem dashboard dedicado). Prepare/reserva não baixam estoque. Doc: `docs/stock-requests-mvp.md` § Efeito estoque. | Fecha backlog “movimentos item + supply + indicadores” | `270` |
| 2026-08-07 | Chamados Indoor | Coleta completa: `POST /{id}/collect` com coletor + comprovante + assinatura; EXIT supply + serial `RESERVED`→`IN_TRANSIT`; evidências em `extra.collection`; `transition`→`COLLECTED` bloqueado; cancel permitido em `READY_FOR_PICKUP` (release sem EXIT). Sem etapa de conferência/embalagem. | Endpoint `/collect`; rule `270` | `270` |
| 2026-08-06 | Chamados Indoor | Reserva hard assimétrica: **supply** no OPEN (`supplies.reserved_quantity` + holds); **serial** só na bipagem (`IN_DEPOT`→`RESERVED`); release em cancel/reject; sem EXIT/movimento nesta fatia. | Migration `f5c6d7e8f9a0`; prepare consome hold | `270` |
| 2026-08-06 | Chamados Indoor | Gate de aprovação: `approval_required` → `AWAITING_APPROVAL`; actions approve / request_complement / reject / resubmit; PATCH só em `AWAITING_COMPLEMENT`; rejeição formal só no gate (não a partir de OPEN); sem notificação externa nesta fatia. | Status seed `f4b5c6d7e8f9`; machine de transições | `270` |
| 2026-08-06 | Chamados Indoor | “Categoria” = **supply vs serializado** (`extra_info.supplies`), **não** o campo `product.category`. Prepare aceita produto diferente do mesmo tipo; `READY_FOR_PICKUP` cobre soma por tipo. | Ajuste da regra de compatibilidade | `270` |
| 2026-08-05 | Chamados Indoor | Novo domínio **stock requests** (preparação de materiais), separado do Claro Indoor (reversa). API `/api/v1/stock-requests`. MVP até `READY_FOR_PICKUP`; packages obrigatórios no prepare; serial via `logistic_stock_item` ou qty via `logistic_stock_supplies`; reserva soft; sem movimento físico no MVP; `COLLECTED` seed-only. | Models `logistic_stock_request*`; rule `270` | `270` |
| 2026-08-03 | Supply Control | Fluxo novo de insumos não serializáveis: saldo em `logistic_stock_supplies`, flag `extra_info.supplies=true`, TRANSFER bilateral (- origem / + destino); Movement API bloqueia insumos | Endpoints `/v1/supplies*`; não misturar com estoque serializado | `260`, `200`, `230` |
| 2026-07-24 | Claro Indoor | Nomenclatura oficial **Indoor** (não Indor): prefixo `/api/v1/claro/indoor`, `process_code=INDOOR`, campos `indoor_code`/`indoor_volumes` | Front Django/Arancia e integradores | `250` |
| 2026-07-24 | Claro Indoor | Check-in: localidade de estoque = GAI do usuário (`location_id` no multipart), **não** coluna da planilha. Distinto de `localidade_atlas` (etapa Atlas / `item.extra_info`) | Planilha check-in só exige `serial`; UI envia GAI da sessão | `250` |
| 2026-06-01 | Catálogo | Allowlist de clientes visíveis no estoque: tabela `logistic_stock_client_catalog` (FK GAI, `enabled`). `GET /clients` filtra catálogo; gestão via `/clients/catalog`. Demais fluxos não bloqueiam GAIs fora do catálogo | Transporte-only não aparece na listagem; romaneio continua com qualquer GAI válido | `230` |
| 2026-06-01 | Arquitetura | Padrão `Endpoint -> Service -> CRUD` é mandatório | Novos endpoints devem delegar regras ao service | Já em rules `010`, `020`, `030`, `040` |
| 2026-06-01 | API | Versão ativa de romaneios para novos fluxos é **v2** (`/api/v2/romaneios`); criação v1 marcada deprecated | Evitar evoluir v1 sem alinhamento | `210-business-romaneios-auto.mdc` |
| 2026-06-01 | Romaneio | Formato de número de romaneio depende do cliente Cielo vs demais (`before_insert` + GAI) | Integrações devem usar `romaneio_number` gerado, não inventar | `210` |
| 2026-06-01 | Romaneio | Conferência de recebimento (`RECEIVE`) depende de flag `ck=True` nos itens | Itens sem CK não entram no finish RECEIVE | `210` |
| 2026-06-01 | Movimentação | Histórico de movimentos é fonte de verdade para elegibilidade em romaneio (último movimento IN, não ADJUST) | Consultas de estoque devem considerar `movement_type` | `200` |
| 2026-06-01 | Cielo | Integração SAP via consulta síncrona é parte do fluxo de entrada e picking | Falhas geram 424 ou `StockErrors` + serial provisório ILG | `220` |
| 2026-06-01 | Config | Secrets em `.env`; `core/config.py` não versionado; CI copia `example-config.py` | Setup local/CI padronizado | `080-security-config-always.mdc` |
| 2026-06-01 | Documentação | `.cursor/business-rules/` é auxiliar; rules ativas ficam em `.cursor/rules/*.mdc` | Processo de descoberta (rule 120) | Não |
| 2026-06-01 | Movimentação | `movement_type=RETURN` e `OrderOrigin.project_name=REVERSA` são campos distintos; não unificar nomenclatura | Rules e novos endpoints devem distinguir os dois campos | `200`, `210`, `230` |
| 2026-06-01 | Romaneio finish | ~~`RomaneioFineshedResponse` mantém `status_rom="DISPATCHED"` fixo~~ → **retornar `status_rom` real** após `update_rom_by_movement` + reload (2026-06-01) | Integradores recebem estado final (`RECEIVED`, `AWAITING_COLLECTION`, etc.) | `210` |
| 2026-06-01 | Segurança | API sem autenticação na camada FastAPI é intencional | Confiança em rede/gateway; não adicionar middleware JWT sem decisão nova | `080` (referência) |
| 2026-06-01 | Romaneio | Status cancelado canônico: **`CANCELLED`**; filtro v2 deve alinhar (não `CANCELADO`) | Listagem v2 e integrações usam mesmo literal do enum | `210` |
| 2026-06-01 | API | **v2** para features novas; **v1** somente manutenção até desligar | Não evoluir v1 sem necessidade | `210` |
| 2026-06-01 | Config | `EVENTS_INTELIPOST` é referência em config; **não usado** pelos fluxos desta API | Não assumir dependência em movimentos/romaneios | `230` |
| 2026-06-01 | Cielo | CHIP: consulta SAP não aplicável; **`product_id` obrigatório** em `IN`; sem ILG em falha SAP | Validação explícita antes de criar item CHIP | `220`, `200` |
| 2026-06-01 | Movimentação | `ignore_status_rule`: uso excepcional (correções), não fluxo normal | Documentar como escape hatch operacional | `200` |
| 2026-06-01 | Catálogo | Origens em banco (`logistic_stock_order_origin`); combinações `client_id=1` documentadas na rule `230` | Filtros respeitam case exato (`sap` vs `SAP`) | `230` |
| 2026-06-01 | Catálogo | Cliente contratante = GAI `arancia_client` (`logistica_groupaditionalinformation`); removida `logistic_stock_client` | `client_id` em produto/origem/romaneio aponta para GAI; `GET /clients` read-only; cadastro no Django | `230` |
| 2026-06-01 | API | Alias legado **`client_id=1` na entrada** (writes) → GAI `nome=CIELO`; persistir id real do GAI; respostas com id GAI | Integrações podem continuar enviando `1`; quem lê id deve aceitar id GAI ou reenviar `1` | `220`, `230` |
| 2026-06-01 | Dados | Migration **`b7e2a91c4d05`**: pós-FK, HG força todos os `client_id` → GAI CIELO; prod corrige `client_id=1` e referências fora de `arancia_client` | Rodar `alembic upgrade head` após `a8f3c2e91b04` em cada ambiente | `230` |
| 2026-06-01 | Romaneio | Numeração Cielo usa prefixo **`AR105`** via `is_cielo_client` (GAI CIELO), não comparação `client_id == 1` no valor persistido | Parser `AR1*` e integrações legadas preservados | `210` |
| 2026-06-01 | Erros | `StockErrors` **somente picking Cielo** hoje | Expansão futura possível; não exigir em todos os fluxos | `220` |
| 2026-06-01 | Romaneio | Seriais duplicados podem existir fisicamente; bloqueio em romaneio ativo (`STARTED`/`READY`) permanece | Regra prioritária em produção | `210` |
| 2026-06-01 | Cielo | Divergência depósito SAP (`LGORT`) vs Arancia é cenário operacional esperado (SAP assíncrono) | Picking grava `StockErrors`; não tratar como bug isolado | `220` |
| 2026-06-01 | Romaneio | Resposta **READY ↔ finish revogada** (explicação incorreta) | Rule `210` atualizada: sem associação READY ao finish; enum + bloqueio STARTED/READY apenas | `210` |
| 2026-06-01 | Romaneio | **`READY` deprecated/legado** — manter no enum e bloqueios existentes; novos fluxos PA→CD usam `AWAITING_COLLECTION` após finish RETURN | Não remover READY; não usar em features novas | `210` |
| 2026-06-01 | Estoque | **Bag-técnico desacoplado de romaneio** — entrada `IN`; consulta auxiliar `GET /items/search-by-movement` por `created_by`; sem relação obrigatória bag↔romaneio | Bag é visão item/movimento, não regra de romaneio | `200` |
| 2026-06-01 | Romaneio | **Validação PA/CD fora da API** — chamador valida via `location.group_id`; API não bloqueia create/finish por PA/CD nesta fase | Helper futuro opcional, não ativado | `210`, `230` |
| 2026-06-01 | Romaneio | **Recebimento parcial** — finish RECEIVE com algum `ck=false` → `PARTIALLY_RECEIVED`; todos `ck=true` → `RECEIVED`; resposta com resumo | Implementado Fase 3 (2026-06-02) | `210` |
| 2026-06-02 | Movimentação / Romaneio | **Fase 2.5 PA→CD**: finish agregado via `advance_romaneio`; `COLLECTION_REQUEST` (mov.) + `COLLECTION_REQUESTED` (rom.); RECEIVE pré **`PRE_RECEIVED`**; RETURN legado rom → **`DISPATCHED`**; Movement API = recebimento/delivery/APP sem orchestration | Breaking: RECEIVE exige rom `PRE_RECEIVED` (não `DISPATCHED`) | `200`, `210` |
| 2026-06-01 | Movimentação | **`COLLECTION_REQUEST`** → rom **`COLLECTION_REQUESTED`** via finish; **`COLLECTION_REQUEST_RECEIVED`** → item **`READY_FOR_COLLECTING`**, rom **`AWAITING_COLLECTION`** | Substitui Fase 2 com `COLLECTION_REQUESTED` na movement API | `200`, `210` |
| 2026-06-01 | Romaneio | **`PARTIALLY_RECEIVED`**: itens pendentes **sem fluxo adicional por enquanto** (P4) | Documentar; evoluir depois se necessário | `210` |
| 2026-06-01 | Romaneio | Falha no finish após `IN_PROCESSING` → **`PROCESSING_ERROR`** + log retentável; RECEIVE parcial valida só `ck=True` (P5) | Fase 1 handler + Fase 3 parcial | `210`, `200` |
| 2026-07-23 | Claro Indoor | GAI origem Indoor = `ClaroReversa` no grupo `arancia_client` (fora do catálogo de estoque); destino = GAI `CLARO`; location = PA/CD | Lookups/create romaneio Indoor | `250` |
| 2026-08-05 | Claro Indoor | **Correção:** origem Indoor = GAI `arancia_PA` **selecionável** (`origins[]`); destino/client = GAI `CLARO`. Remover GAI `ClaroReversa` após migrar FKs/eventos (migration `e2f3a4b5c6d7`, HG primeiro) | Substitui decisão 2026-07-23 sobre ClaroReversa | `250` |
| 2026-07-23 | Claro Indoor | UI hub + APIs de suporte: dashboard, lookups create (PA/CD + ClaroReversa + destino Claro), busca itens/romaneios, volumes sequenciais, confirm com totais/409 duplicata, giro default `baixo` obrigatório | Spec `docs/claro-indoor-ui-screens.md`; endpoints novos sob `/api/v1/claro/indoor` | `250` |
| 2026-07-23 | Claro Indoor | Catálogo de status/transições em banco (`process_code=INDOOR` seed); `romaneio.extra_info` JSONB; `RomaneioEvent` global + instrumentação PA→CD/v2; GET `/api/v1/claro/indoor/status-catalog*` | Sequência Indoor via tabelas; auditoria obrigatória com `created_by`/`created_at` | `250` |
| 2026-07-23 | Claro Indoor | Uploads 1–3 all-or-nothing com `dry_run`; contrato unitário no `last_in`; export `IN_DEPOT` usa `is_finalizer=false` do catálogo para `EM_ROMANEIO` | Service `claro_indoor` concentra etapas 1–3 e consultas; endpoints HTTP no follow-up | `250` |
| 2026-07-23 | Romaneio | **Pendência:** migrar hardcodes PA→CD (`_ADVANCE_ROMANEIO_TRANSITIONS`) para `process_code=PA_CD` nas mesmas tabelas de catálogo | Nesta leva só INDOOR é seedado/validado | `210`, `250` |
| 2026-06-12 | Estoque / Bag | **Consulta bag por `order_number`** — último mov. `IN` ou `COLLECTED`; chave distinta de `search-by-movement` (`created_by`) | `GET /api/v1/items/bag`; elegibilidade fixa no backend | `200` |
| 2026-06-12 | Estoque / Bag | **Remoção da bag** — movimento dedicado `BAG_REMOVED` + status `REMOVED_FROM_BAG` (não `TRANSFER` + `ignore_status_rule`) | Item some da consulta bag; rastreável em validações futuras | `200` |
| 2026-06-12 | Estoque / Bag | **Troca de serial na bag** — UPDATE direto em `item.serial`, sem novo movimento; auditoria em `item.extra_info.bag_serial_replaced`; duplicado → 409 | Histórico de movimentos intacto no mesmo `item_id` | `200` |
| 2026-06-12 | Estoque / Bag | **Baixa TOA (CLR)** — flag `baixa_toa` em `last_movement.extra_info`; PATCH sem novo movimento; POST na entrada opcional | Fluxo interno TOA para OS prefixo `CLR` | `200`, `docs/bag-tecnico-spec.md` |
| 2026-06-15 | Movimentação / Claro | **`COLLECTED` + Claro + OS `CLR*`** — default `baixa_toa=false` centralizado em `MovementService.create_movement` (Movement API + bag) | Regra única; integrador não sobrescreve via `extra_info` | `200`, `docs/bag-tecnico-spec.md` |
| 2026-06-15 | Estoque / Bag | **Entrada bag = COLLECTED fixo** — POST sem `movement_type`/`product_id`; CLR nasce com `baixa_toa=false`; PATCH baixa TOA em lote (`/bag/{order}/baixa-toa`) com sucesso parcial | Breaking change para integradores da bag | `200`, `docs/bag-tecnico-spec.md` |
| 2026-07-22 | Estoque / Bag | **Location via `auth_app`** — `created_by` = `auth_app.uid` → `gai_id` como `to_location_id` no POST add serial (override explícito permitido) | Body mínimo sem location; técnico precisa de `gai_id` | `docs/bag-tecnico-spec.md`, `240` |
